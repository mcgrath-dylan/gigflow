You are evaluating a freelance gig posting for Dylan, a Data & Automation Specialist. He builds web scrapers, Python scripts, data pipelines, API integrations, and handles data cleanup/analysis. Claude AI handles ~80% of the actual coding and production work; Dylan's role is QA (running the code, spot-checking output), client communication, and delivery.

Evaluate the following gig posting and return a JSON object with exactly this structure:
{
  "clarity_score": <1-10>,
  "ai_deliverability_score": <1-10>,
  "qa_feasibility_score": <1-10>,
  "estimated_dylan_hours": <number>,
  "budget_mentioned": <true|false>,
  "red_flags": [<list of short strings, or empty list>],
  "recommendation": <"BID" | "MAYBE" | "SKIP">,
  "reasoning": "<one sentence>",
  "gig_type": <"web-scraping" | "python-script" | "api-integration" | "data-cleanup" | "analysis" | "general-short">
}

Scoring criteria:
- clarity_score: Is the deliverable well-defined? 10 = crystal clear scope ("scrape X from Y, deliver as CSV"), 1 = completely vague ("build me something")
- ai_deliverability_score: Can Claude produce 80%+ of this? 10 = standard scripting/data task, 1 = requires human presence, proprietary tools, or creative judgment
- qa_feasibility_score: Can Dylan verify the output by running the code locally and spot-checking results? 10 = run script, check output file. 1 = requires deep domain expertise, access to client systems, or complex integration testing Dylan can't replicate
- estimated_dylan_hours: Dylan's time only (running code, QA, comms, delivery) — NOT total project hours
- red_flags: Look for: vague scope, "build a full application", no budget, new account, ongoing/VA roles, requires meetings, requires access to client servers/credentials, "test project" language, extremely low budget for complex work
- recommendation: BID if clarity>=7 AND ai_deliverability>=7 AND qa_feasibility>=7 AND no major red flags. SKIP if any score <5 or major red flags. MAYBE otherwise.
- gig_type: Best-fit template category. "web-scraping" for scraping/crawling tasks. "python-script" for automation scripts and bots. "api-integration" for API connection work. "data-cleanup" for data formatting/cleaning. "analysis" for data analysis or reporting. "general-short" for anything else.

Return ONLY the JSON object. No explanation, no markdown, no extra text.
